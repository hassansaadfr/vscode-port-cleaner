name: Publish VS Code Extension

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0, v1.1.0, etc.

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile TypeScript
        run: npm run compile

      - name: Install vsce
        run: npm install -g @vscode/vsce

      - name: Package extension
        run: vsce package

      - name: Publish to VS Code Marketplace
        run: vsce publish -p ${{ secrets.VSCE_PAT }}
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: '*.vsix'
          body: |
            ## VS Code Port Cleaner ${{ steps.get_version.outputs.VERSION }}

            ### Installation
            1. Download the `.vsix` file below
            2. Open VS Code
            3. Go to Extensions view (Ctrl+Shift+X / Cmd+Shift+X)
            4. Click "..." menu → "Install from VSIX..."
            5. Select the downloaded file

            Or install directly from the [VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=hassansaadfr.port-cleaner)

            ### Changes
            See [CHANGELOG.md](https://github.com/hassansaadfr/vscode-port-cleaner/blob/main/CHANGELOG.md) for full details.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish success notification
        if: success()
        run: |
          echo "✅ Extension published successfully!"
          echo "📦 Version: ${{ steps.get_version.outputs.VERSION }}"
          echo "🔗 Marketplace: https://marketplace.visualstudio.com/items?itemName=hassansaadfr.port-cleaner"
